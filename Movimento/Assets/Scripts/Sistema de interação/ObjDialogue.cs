using UnityEngine;

/* public class ObjDialogue : MonoBehaviour
{
    [SerializeField] private string _dialogue;
    [SerializeField] private float _screenTime = 2f;
    [SerializeField] private InteractionDialoguePrompt interactionDialoguePrompt;

    public bool Interact(Interactor interactor)
    {
        interactionDialoguePrompt.SetText(_dialogue);
        interactionDialoguePrompt.ActivateDialog();
        Invoke(nameof(CloseDialogue), _screenTime);
        return true;
    }

    private void CloseDialogue()
    {
        interactionDialoguePrompt.CloseUi();
    }
} */
